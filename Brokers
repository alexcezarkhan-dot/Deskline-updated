<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Forex &amp; Trading Broker Reviews and Guides | Deskline</title>
<meta name="description" content="Broker reviews, comparisons, and guides to help you choose a forex, crypto, or CFD trading broker — regulation, spreads, execution, and withdrawal reliability explained.">
<meta name="keywords" content="forex broker review, best forex broker, trading broker comparison, broker regulation, ECN broker, STP broker">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
  :root{
    --bg:#0E1116; --panel:#151A22; --panel-2:#12161D;
    --line-soft:rgba(255,255,255,0.08); --ink:#EDEFF3; --dim:#7C8598; --gold:#E8B15C;
  }
  *{margin:0;padding:0;box-sizing:border-box;}
  body{ background:var(--bg); color:var(--ink); font-family:'Space Grotesk',sans-serif; min-height:100vh; }
  .mono{ font-family:'JetBrains Mono', monospace; }

  nav{ display:flex; justify-content:space-between; align-items:center; padding: 1.2rem 6vw; border-bottom:1px solid var(--line-soft); }
  .wordmark{ font-size:1.25rem; font-weight:700; letter-spacing:-0.01em; }
  .wordmark span{ color:var(--gold); }
  .navlinks{ display:flex; gap:1.8rem; font-family:'JetBrains Mono', monospace; font-size:.75rem; letter-spacing:.04em; }
  .navlinks a{ color:var(--dim); text-decoration:none; transition:color .15s; }
  .navlinks a:hover{ color:var(--ink); }
  .navlinks a.active{ color:var(--gold); }

  .pagehead{ padding: 2rem 6vw 1.6rem; }
  .pagehead h1{ font-size: clamp(1.6rem, 3vw, 2.2rem); font-weight:700; letter-spacing:-0.01em; }
  .pagehead p{ font-family:'JetBrains Mono', monospace; font-size:.75rem; color:var(--dim); margin-top:.4rem; }

  .posts{
    padding: 0 6vw 3rem;
    display:grid;
    grid-template-columns: repeat(auto-fit, minmax(280px,1fr));
    gap: 1.2rem;
  }
  .post-card{
    background: var(--panel);
    border:1px solid var(--line-soft);
    border-radius:8px;
    padding: 1.5rem;
    text-decoration:none;
    color:var(--ink);
    display:flex;
    flex-direction:column;
    transition: border-color .15s, transform .15s;
  }
  .post-card:hover{ border-color: var(--gold); transform: translateY(-2px); }
  .post-tag{
    font-family:'JetBrains Mono', monospace;
    font-size:.65rem;
    letter-spacing:.08em;
    text-transform:uppercase;
    color: var(--gold);
    margin-bottom:.7rem;
  }
  .post-title{ font-size:1.15rem; font-weight:600; margin-bottom:.6rem; line-height:1.3; }
  .post-excerpt{ font-size:.88rem; color:var(--dim); line-height:1.5; flex:1; }
  .post-date{
    font-family:'JetBrains Mono', monospace;
    font-size:.7rem; color:var(--dim);
    margin-top:1rem;
    padding-top:1rem;
    border-top:1px solid var(--line-soft);
  }
  .empty{
    padding: 0 6vw 3rem;
    font-family:'JetBrains Mono', monospace;
    font-size:.85rem;
    color:var(--dim);
  }

  .seo-content{
    padding: 1rem 6vw 3rem;
    max-width: 900px;
  }
  .seo-content h2{ font-size:1.1rem; font-weight:600; margin: 1.6rem 0 .6rem; }
  .seo-content h2:first-child{ margin-top:0; }
  .seo-content p{ font-size:.92rem; line-height:1.7; color:#B7BECC; margin-bottom:.8rem; }

  footer{
    text-align:center; padding: 1.8rem; font-family:'JetBrains Mono', monospace;
    font-size:.65rem; color: var(--dim); border-top:1px solid var(--line-soft);
  }

  /* ---------------- DeskAi floating chatbox ---------------- */
  .deskai-toggle{
    position: fixed;
    right: 24px; bottom: 24px;
    z-index: 999;
    width: 58px; height: 58px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--gold), #B9803A);
    border: none;
    cursor: pointer;
    display:flex; align-items:center; justify-content:center;
    box-shadow: 0 4px 18px rgba(0,0,0,0.4);
    font-size: 1.4rem;
    transition: transform .15s;
  }
  .deskai-toggle:hover{ transform: scale(1.06); }
  .deskai-toggle .dot{
    position:absolute; top:6px; right:6px;
    width:10px; height:10px; border-radius:50%;
    background:#4ADE80; border:2px solid var(--bg);
  }

  .deskai-panel{
    position: fixed;
    right: 24px; bottom: 96px;
    z-index: 999;
    width: 360px;
    max-width: calc(100vw - 32px);
    height: 520px;
    max-height: calc(100vh - 140px);
    background: var(--panel);
    border: 1px solid var(--line-soft);
    border-radius: 12px;
    display: none;
    flex-direction: column;
    overflow: hidden;
    box-shadow: 0 10px 40px rgba(0,0,0,0.5);
  }
  .deskai-panel.open{ display:flex; }

  .deskai-header{
    display:flex; align-items:center; justify-content:space-between;
    padding: .9rem 1.1rem;
    border-bottom: 1px solid var(--line-soft);
    background: var(--panel-2);
  }
  .deskai-header-title{ display:flex; align-items:center; gap:.5rem; }
  .deskai-header-title .mark{
    width:26px; height:26px; border-radius:50%;
    background: linear-gradient(135deg, var(--gold), #B9803A);
    display:flex; align-items:center; justify-content:center;
    font-weight:700; color:#1A1305; font-size:.75rem;
  }
  .deskai-header-title span{ font-weight:600; font-size:.9rem; }
  .deskai-header-sub{ font-family:'JetBrains Mono', monospace; font-size:.62rem; color:var(--dim); }
  .deskai-close{
    background:transparent; border:none; color:var(--dim); cursor:pointer; font-size:1.1rem;
    line-height:1; padding:.2rem;
  }
  .deskai-close:hover{ color:var(--ink); }

  .deskai-messages{
    flex:1;
    overflow-y:auto;
    padding: 1rem 1.1rem;
    display:flex; flex-direction:column; gap:.8rem;
  }
  .deskai-msg{
    max-width: 85%;
    padding: .65rem .85rem;
    border-radius: 10px;
    font-size: .85rem;
    line-height: 1.55;
    white-space: pre-wrap;
  }
  .deskai-msg.user{
    align-self:flex-end;
    background: var(--gold);
    color: #1A1305;
    border-bottom-right-radius:2px;
  }
  .deskai-msg.assistant{
    align-self:flex-start;
    background: var(--panel-2);
    color: #D7DAE1;
    border-bottom-left-radius:2px;
  }
  .deskai-msg.thinking{
    align-self:flex-start;
    background: var(--panel-2);
    color: var(--dim);
    font-family:'JetBrains Mono', monospace;
    font-size:.75rem;
  }
  .deskai-msg.err{
    align-self:flex-start;
    background: rgba(251,107,91,0.1);
    color: #FB6B5B;
    font-family:'JetBrains Mono', monospace;
    font-size:.75rem;
  }

  .deskai-welcome{
    font-size:.82rem; color:var(--dim); line-height:1.6; padding: .2rem .1rem 0;
  }
  .deskai-quick{
    display:flex; flex-wrap:wrap; gap:.4rem; padding: 0 1.1rem .9rem;
  }
  .deskai-quick button{
    font-family:'JetBrains Mono', monospace;
    font-size:.66rem;
    background:transparent;
    border:1px solid var(--line-soft);
    color:var(--dim);
    padding:.35rem .65rem;
    border-radius:5px;
    cursor:pointer;
  }
  .deskai-quick button:hover{ border-color:var(--gold); color:var(--gold); }

  .deskai-input-row{
    display:flex; gap:.5rem;
    padding: .8rem;
    border-top:1px solid var(--line-soft);
    background: var(--panel-2);
  }
  .deskai-input{
    flex:1;
    background: var(--panel);
    border:1px solid var(--line-soft);
    border-radius:6px;
    padding:.6rem .75rem;
    color:var(--ink);
    font-family:'Space Grotesk',sans-serif;
    font-size:.85rem;
    outline:none;
  }
  .deskai-input:focus{ border-color:var(--gold); }
  .deskai-send{
    background: var(--gold);
    border:none;
    border-radius:6px;
    width:38px;
    color:#1A1305;
    font-weight:700;
    cursor:pointer;
    flex-shrink:0;
  }
  .deskai-send:hover{ opacity:.9; }

  @media (max-width: 480px){
    .deskai-panel{ right:16px; left:16px; width:auto; bottom:88px; }
    .deskai-toggle{ right:16px; bottom:16px; }
  }

</style>
</head>
<body>

<nav>
  <div class="wordmark">Desk<span>line</span></div>
  <div class="navlinks">
    <a href="../index.html">Market</a>
    <a href="../calculator.html">Calculator</a>
    <a href="../news.html">News</a>
    <a href="../calendar.html">Economic Calendar</a>
    <a href="index.html" class="active">Brokers</a>
  </div>
</nav>

<div class="pagehead">
  <h1>Brokers</h1>
  <p>BROKER REVIEWS, COMPARISONS &amp; GUIDES</p>
</div>

<div class="posts" id="postsGrid"></div>
<div class="empty" id="emptyMsg" style="display:none;">No posts published yet — check back soon.</div>

<section class="seo-content">
  <h2>Choosing a Forex or Trading Broker</h2>
  <p>Read broker reviews and guides covering regulation, spreads, execution models (A-Book, B-Book, ECN, STP), and withdrawal reliability — the factors that actually matter when picking a forex, crypto, or CFD broker.</p>
</section>

<footer>Deskline · Brokers · <a href="../about.html">About</a> · <a href="../contact.html">Contact</a></footer>

<script>
fetch('posts.json')
  .then(r => r.json())
  .then(posts => {
    const grid = document.getElementById('postsGrid');
    if(!posts.length){
      document.getElementById('emptyMsg').style.display = 'block';
      return;
    }
    // newest first
    posts.sort((a,b) => (b.date||'').localeCompare(a.date||''));
    grid.innerHTML = posts.map(p => `
      <a class="post-card" href="posts/${p.slug}.html">
        <div class="post-tag">${p.tag || 'Guide'}</div>
        <div class="post-title">${p.title}</div>
        <div class="post-excerpt">${p.excerpt || ''}</div>
        <div class="post-date">${p.date || ''}</div>
      </a>
    `).join('');
  })
  .catch(() => {
    document.getElementById('emptyMsg').textContent = 'Could not load posts.';
    document.getElementById('emptyMsg').style.display = 'block';
  });
</script>


<button class="deskai-toggle" id="deskaiToggle" aria-label="Open DeskAi">
  💬<span class="dot"></span>
</button>

<div class="deskai-panel" id="deskaiPanel">
  <div class="deskai-header">
    <div class="deskai-header-title">
      <div class="mark">D</div>
      <div>
        <div><span>DeskAi</span></div>
        <div class="deskai-header-sub">Powered by Gemini</div>
      </div>
    </div>
    <button class="deskai-close" id="deskaiClose">✕</button>
  </div>
  <div class="deskai-messages" id="deskaiMessages">
    <div class="deskai-welcome" id="deskaiWelcome">Hi! I'm DeskAi — ask me about choosing a broker, regulation, or trading terms.</div>
  </div>
  <div class="deskai-quick" id="deskaiQuick"><button data-q="What should I look for when choosing a forex broker?">How do I choose a broker?</button></div>
  <div class="deskai-input-row">
    <input type="text" class="deskai-input" id="deskaiInput" placeholder="Ask DeskAi…" autocomplete="off">
    <button class="deskai-send" id="deskaiSend">→</button>
  </div>
</div>

<script>
window.getDeskAiContext = async function(){ return null; };

/* ---------------- DeskAi floating chatbox ---------------- */
(function(){
  const SECTION = "general";
  const toggle = document.getElementById('deskaiToggle');
  const panel = document.getElementById('deskaiPanel');
  const closeBtn = document.getElementById('deskaiClose');
  const messagesEl = document.getElementById('deskaiMessages');
  const quickEl = document.getElementById('deskaiQuick');
  const inputEl = document.getElementById('deskaiInput');
  const sendBtn = document.getElementById('deskaiSend');

  let history = [];
  let opened = false;

  toggle.addEventListener('click', () => {
    opened = !opened;
    panel.classList.toggle('open', opened);
    if(opened) inputEl.focus();
  });
  closeBtn.addEventListener('click', () => {
    opened = false;
    panel.classList.remove('open');
  });

  function addMessage(role, text){
    const div = document.createElement('div');
    div.className = 'deskai-msg ' + role;
    div.textContent = text;
    messagesEl.appendChild(div);
    messagesEl.scrollTop = messagesEl.scrollHeight;
    return div;
  }

  async function sendToDeskAi(question){
    if(!question.trim()) return;
    document.getElementById('deskaiWelcome')?.remove();
    quickEl.style.display = 'none';
    addMessage('user', question);
    history.push({ role:'user', text: question });
    const thinkingEl = addMessage('thinking', 'DeskAi is thinking…');

    let context = null;
    if(typeof window.getDeskAiContext === 'function'){
      try{ context = await window.getDeskAiContext(); } catch(e){}
    }

    try{
      const res = await fetch('/.netlify/functions/deskai', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ section: SECTION, question, context, history: history.slice(0,-1) })
      });
      const data = await res.json();
      thinkingEl.remove();
      if(data.error){
        addMessage('err', "DeskAi isn't available right now — it may not be set up on this site yet.");
        return;
      }
      addMessage('assistant', data.answer);
      history.push({ role:'assistant', text: data.answer });
    } catch(e){
      thinkingEl.remove();
      addMessage('err', "DeskAi isn't available right now — it may not be set up on this site yet.");
    }
  }

  sendBtn.addEventListener('click', () => {
    const q = inputEl.value.trim();
    if(!q) return;
    inputEl.value = '';
    sendToDeskAi(q);
  });
  inputEl.addEventListener('keydown', (e) => {
    if(e.key === 'Enter'){
      const q = inputEl.value.trim();
      if(!q) return;
      inputEl.value = '';
      sendToDeskAi(q);
    }
  });
  quickEl.querySelectorAll('button').forEach(btn => {
    btn.addEventListener('click', () => sendToDeskAi(btn.dataset.q));
  });
})();

</script>
</body>
</html>
